<?php
    /**
     * uninstall.php 
     * - drop module tables (or keep them for safety)
     * - remove feature instances from all apps
     * - remove module-specific options
     *
     */

    // $db = Zend_Db_Table::getDefaultAdapter();
    // $db->query("DROP TABLE IF EXISTS geooffers_offer_location");
    // $db->query("DROP TABLE IF EXISTS geooffers_location");
    // $db->query("DROP TABLE IF EXISTS geooffers_offer");
